#include <stdio.h>      //just something (usually original functions) should be put at the head.

int main()              //main function is the only entrance of a C program whose return value is usually an integer.
{
    printf("hello");    //every executing sentence should have a semicolon at the end.
    return 0;           //even if doesn't matter, it represents sort of attitude.
}